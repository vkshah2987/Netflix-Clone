# Environment Variables for Netflix Clone

# TMDB API Token (Server-side only - used by Netlify Functions)
# Get your token from: https://www.themoviedb.org/settings/api
# This should be a "Read Access Token" (Bearer token, v4 auth)
TMDB_V4_TOKEN=your_tmdb_bearer_token_here

# Note: Do NOT use VITE_ prefix for this token!
# VITE_ prefix exposes variables to the browser bundle.
# The TMDB_V4_TOKEN is only used server-side in Netlify Functions.
